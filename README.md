# Jani's TS Starter

Strict TypeScript 7 / tsgo starter with bundler module resolution, native ESM package syntax, oxfmt, oxlint, Nix, and direnv.

```sh
direnv allow
corepack pnpm install
corepack pnpm run check
```
